<!-- Generated by `make docs` from daemon.toml + skill/SKILL.md. Do not edit by hand. -->

# slack-standup

Generate the daily standup and post it to a Slack channel (Mon–Fri, once per day).

- **Command:** `/slack-standup`
- **Schedule:** 11:00
- **Source:** none
- **Required inputs:** `channel_id`
- **Backend:** `claude`

## Inputs

| Input | Default | Notes |
|-------|---------|-------|
| `channel_id` | `CHANGE_ME` | Slack channel the standup posts to. The Slack MCP tools come from the user-level `slack` plugin (already OAuth'd) — no daimon mcp entry needed. |
| `channel_name` | `CHANGE_ME` |  |
| `tz` | `America/New_York` | Timezone for the weekday / once-per-day gate — keep in sync with schedule tz. |
| `standup_command` | `/standup` | Skill that generates the standup content; its output is posted verbatim. |

## Gate

`discover.sh` decides whether the daemon fires on a given run.

See [skill/SKILL.md](skill/SKILL.md) for the full prompt.
