# Copy to daemon.local.toml (gitignored) and set values for THIS machine.
[daemon]
working_dir = "~/code"   # a directory containing the repos your stories touch

[inputs]
repos = ["your-repo"]    # repos under working_dir this daemon may implement in
