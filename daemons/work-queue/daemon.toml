[daemon]
backend = "claude"
source = "shortcut"
schedule = { minutes = [8, 38] }
command = "/work-queue"
stuck_after = 2700
required_inputs = ["ready_label", "skip_label", "in_progress_state"]

[inputs]
repos = []
base = "main"
max_open_prs = 2
